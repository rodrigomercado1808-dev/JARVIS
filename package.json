{
  "name": "jarvis-ai-assistant",
  "version": "1.0.0",
  "private": true,
  "description": "JARVIS personal AI assistant with vanilla JavaScript backend and frontend",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/local-brain.js && node --check src/embedded-brain.js && node --check src/neural-engine.js",
    "brain:inspect": "node scripts/inspect-brain.js",
    "brain:test": "node scripts/test-brain.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@fastify/formbody": "^8.0.1",
    "@fastify/static": "^8.0.0",
    "dotenv": "^16.4.7",
    "fastify": "^5.2.1",
    "firebase-admin": "^13.0.2",
    "zod": "^3.24.1"
  }
}
