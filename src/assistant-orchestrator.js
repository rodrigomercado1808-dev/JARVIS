const { normalize } = (() => ({ normalize: text => String(text).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, '') }))();
function inspect(message, history = []) { const q = normalize(message); const hasContext = history.length > 0; if (/^(hola|buenas|hey|saludos)\b/.test(q)) return { mode: 'conversation', reason: 'greeting', useWeb: false, useMemory: hasContext }; if (/(que puedes hacer|qué puedes hacer|capacidades|habilidades)/.test(q)) return { mode: 'capabilities', reason: 'capabilities', useWeb: false, useMemory: false }; if (/(que hora|qué hora|fecha de hoy|dia de hoy|día de hoy|hora actual)/.test(q)) return { mode: 'datetime', reason: 'current-time', useWeb: false, useMemory: false }; if (/^(cuanto es|cuánto es|calcula|calcular)\b/.test(q) && /^[\s\d+\-*/().%]+$/.test(q.replace(/^(cuanto es|cuánto es|calcula|calcular)\s*/, ''))) return { mode: 'calculator', reason: 'arithmetic', useWeb: false, useMemory: false }; if (/(recorda|recordá|recuerd|te dije|hablamos antes|contexto anterior|mi proyecto|mi nombre)/.test(q)) return { mode: 'memory', reason: 'personal-context', useWeb: false, useMemory: true }; if (/^(ejecuta|ejecutá|corre|corré|abre|abrí|borra|borrá|elimina|eliminá)/.test(q)) return { mode: 'safe-action', reason: 'action-request', useWeb: false, useMemory: true, requiresConfirmation: true }; return { mode: hasContext ? 'contextual-answer' : 'knowledge-answer', reason: 'conversation', useWeb: null, useMemory: true }; }
function capabilities() { return 'Puedo conversar en un único chat, recordar contexto, explicar temas, escribir y revisar código, hacer cálculos seguros, consultar fecha y hora, aprender investigaciones y guardar memoria cifrada cuando iniciás sesión. No ejecuto acciones destructivas sin confirmación.'; }
function persona(text, plan) { if (plan.mode === 'conversation' || plan.mode === 'capabilities') return text; return `Entendido. ${text}`; }
module.exports = { inspect, capabilities, persona };
